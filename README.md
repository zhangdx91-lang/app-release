# 随手记更新发布

此仓库存放随手记 App 的版本发布文件（APK 安装包、Web 热更新包、签名清单）。

应用内"设置 → 更新版本"通过固定地址 `releases/latest/download/...` 检查和下载新版本，无需登录。

- 源代码仓库：私密，不对外开放
- 本仓库只有发布产物，不含任何源代码和密钥
- 每个版本的清单经 Ed25519 签名，产物带 SHA-256 校验，App 端验签后才安装
