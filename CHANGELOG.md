# 随手记 更新日志

> 本文档为**权威版**：每次发版时把新版本段落加在最顶部（与本仓库代码同一提交），再按 发版流程.md 第 5 步同步到 app-release 仓库。
> 编号说明：V1.0.19 与 V1.0.20 各有**两段**（2026-08~09 期间编号曾重复使用），各段以日期区分；V1.0.21 起编号已规范。

## V1.0.37 (2026-09-12)

### 改进：iPhone 主屏 App 打开不再白屏干等
- 从点主屏图标到进入界面，全程有画面反馈，不再是「坏了似的」纯白屏：
  1. **启动瞬间**显示「随手记」logo 启动画面（iOS 官方启动图机制 `apple-touch-startup-image`，12 种分辨率覆盖全部 iPhone 机型）
  2. **程序加载期间**显示 logo + 「加载中…」占位（内联在页面里，秒出），加载完自动进入界面
- 白屏原因回顾：主屏 App 与 Safari 存储完全隔离，首次打开（或每次更新后第一次打开）需从零下载约 300KB 程序代码，国内网络要几秒，此前全程无提示
- 等待时间本身不变（物理下载），但每 1 秒都有画面交代；之后打开走本地缓存依旧秒开
- Android 版同步受益：启动等待期间也显示 logo 加载占位

***

## V1.0.36 (2026-09-12)

### 修复：iOS PWA 主屏图标显示页面缩略图
- 修复把网页版添加到 iPhone 主屏幕时，主屏图标显示当前页面截图的问题（应该显示项目 logo）
- 原因：iOS 不完全遵守 W3C manifest 的 icons 字段，优先用 `<link rel="apple-touch-icon">` 标签；项目之前没加这个标签，iOS fall back 到页面截图
- 已修复 `index.html`：新增 2 行 `apple-touch-icon` 链接（192x192 + 512x512），引用 `public/icons/` 现有的 logo 图标
- 已构建并推送到 gh-pages 分支，CDN 已刷新
- 现有 PWA 用户需要**重新添加主屏图标**才能看到新 logo（旧的 PWA 缓存了旧 sw.js）

### 改进：发版流程加固
- `docs/发版流程.md` 新增「第 0a 步：发版前创建任务清单」强制要求——AI 必须先用 TaskCreate 创建 13 项任务清单再开始执行，避免再次发生跳步（如 V1.0.35 漏推 gh-pages）
- `docs/授权码操作手册.md` 新增「同步本地清单」强制规则——每次发码后必须立即同步 `项目PRD/随手记/授权码/授权码清单.md`

***

## V1.0.35 (2026-09-11)

### 修复：激活码一直激活失败的硬伤（严重）
- 修复从 2026-09-05 授权系统上线以来，所有平台所有用户激活必失败的问题
- 原因：后端校验台账 JSON 时用的是 `ledgerVersion` 字段名，但生成端写入阿里云 OSS 用的是 `schemaVersion`——字段名不一致，校验永远失败，返回 `internal_error`
- 已修复 `server/lib/ledger.js`，同时兼容两种字段名（不破坏台账历史数据），并已重新部署阿里云 FC
- 配合本次发版的授权页 UX 改进，所有未激活用户现在可以正常激活了

### 修复：激活页异常时卡死没反应
- 修复「点激活授权码 → 弹联网确认 → 继续」之后页面卡死在「验证中…」没有任何提示的问题（用户描述为「没反应」）
- 原因：`LicensePage.vue` 的 `activate()` / `deactivate()` 函数没有 try/catch 兜底，遇未预期异常会卡死
- 现在异常时显示红字提示并复位 busy，不会再哑巴死

***

## V1.0.34 (2026-09-10)

### 改进：更新结果改为弹窗提示，并与授权复查合并
- 更新完成刷新后，「已成功更新到 <版本号>，可正常使用」从顶部浮条提示改为居中弹窗，信息更醒目
- 更新成功后的授权复查结果与该弹窗合并展示：授权正常 → 弹「版本更新成功」；授权失效 → 弹失效弹窗（版本号 + 失效原因 + 联系作者二维码），两种结果都在刷新后的新页面统一呈现，不再出现在旧页面弹到一半被刷新打断的情况

***

## V1.0.33 (2026-09-10)

### 修复：更新成功后刷新仍显示旧版本（重要）
- 修复网页版「检查更新」显示下载成功、点击我知道了之后，刷新出来的页面仍是旧版本的问题（此前需要再检查一次才能真正换到新版）
- 原因：新版缓存在接管的一刻没有立即接管已打开的页面，刷新时机落在接管完成之前，拿到的是旧资源；现已让新版缓存激活后立即接管

### 改进：更新进度展示节奏
- 更新弹窗每个步骤至少停留 1 秒再进入下一步，不再一闪而过
- 新增「更新成功」提示：更新完成刷新后，页面会弹出「已成功更新到 <版本号>，可正常使用」

***

## V1.0.32 (2026-09-10)

- 无功能变化：更新流程展示的验证版本（配合录屏检查「发现新版本 → 新版本下载中 → 下载成功」的过渡节奏）

***

## V1.0.31 (2026-09-10)

### 改进：发现新版本后立即进入「下载中」
- 修复网页版更新时长时间停留在「发现新版本」步骤的问题：此前下载前的联网检查（国内网络较慢）被算在"发现新版本"阶段内
- 现在「发现新版本」只闪现一下，随后的联网检查和资源下载全部归入「新版本下载中」步骤，进度展示不再卡顿

***

## V1.0.30 (2026-09-10)

### 改进：更新弹窗补上「下载中」过渡
- 修复网页版更新时进度弹窗从「正在检查中」直接跳到「下载成功」的问题：下载期间现在会显示「新版本下载中」步骤
- 网页版的下载由浏览器后台完成，系统不提供真实百分比，此阶段显示「下载中…」（安卓热更新仍有精确百分比和进度条）

***

## V1.0.29 (2026-09-10)

### 修复：iOS 网页版更新后点击无反应
- 修复 iPhone 网页版「检查更新」完成后，页面出现假死的问题：点底部导航（流水/报表/记一笔）和页面链接均无反应，重新打开才恢复
- 原因是新版缓存在接管的一瞬间立即刷新页面，iOS 会偶发产出一个事件失灵的假死页面；现在刷新前等待一小拍避开这个时机，更新后可直接正常使用
- 安卓 App 不受此问题影响（走的是另一套更新机制），无需更新

***

## V1.0.28 (2026-09-09)

### 移除：复制备份到剪贴板按钮
- 移除 V1.0.24 加入的「复制备份到剪贴板」按钮：实测在 iPhone 桌面应用里复制不可靠（系统剪贴板受限），点按会提示「复制失败」
- 「备份并分享」修复后已可正常使用（不支持分享的环境自动改为下载文件），剪贴板通道失去存在必要，予以移除

***

## V1.0.27 (2026-09-09)

### 修复：更新刷新后顶栏返回按钮没反应
- 修复网页版更新完成自动刷新后，左上角返回按钮点击无反应的问题：刷新会让 iOS 丢失「上一页」记录，纯后退动作就卡死不动
- 返回按钮补上与手势返回一致的兜底：有上一页就退回，没有则直接回首页

***

## V1.0.26 (2026-09-09)

### 修复：网页版「检查更新」一直提示已是最新（重要）
- 修复发布脚本丢失更新清单推送步骤的问题：V1.0.20~V1.0.25 期间网页版「检查更新」按版本号拉取的清单一直停留在 V1.0.19，导致永远提示「已是最新」，用户只能靠反复关闭重开触发浏览器自动更新
- 发布脚本已补回该步骤并增加发布后自动校验（清单未生效则发版视为失败），此后「设置 → 更新版本 → 检查更新」恢复正常，点一次即可更新

***

## V1.0.25 (2026-09-09)

### 改进
- 无功能变化：本次为发布流程验证版本（调整发版顺序，让网页版「检查更新」点一次即可更新成功，不再出现"看到新版本却下载到旧文件"）

***

## V1.0.24 (2026-09-09)

### 修复：网页版「备份并分享」失败
- 修复 iOS「添加到主屏幕」的 PWA 点「备份并分享」报 `The object can not be cloned.` 的问题：网页版 IDB 仓储层原本对 Pinia reactive proxy 调用 `structuredClone` 会炸，现改用 JSON 深拷贝，写入路径不再失败
- 修复 `navigator.canShare` 在不支持文件分享的环境（iOS 桌面 PWA、headless 浏览器）抛错而非返回 false 的问题：先 try-catch 探测，不支持时透明降级走普通下载，按钮不会完全失效

### 新增：复制备份到剪贴板
- 网页版备份页新增「复制备份到剪贴板」按钮，仅网页版显示：iOS 桌面 PWA 没有可用的系统分享面板，作为兜底通道
- 点击后把完整 CSV 备份内容复制到剪贴板，用户到微信/备忘录粘贴即可保存到手机
- 备份内容同样不含设备标识与授权码

***

## V1.0.23 (2026-09-07) — 测试版

### 更新流程重做（按产品时序图）
- 新增居中进度弹窗（5 步进度：已连接网络 → 正在检查中 → 发现新版本 → 下载中[百分比] → 下载成功）
- 取消按钮（放弃更新）：bundle 路径调用 capgo delete 清理已落地文件；APK 路径调用 Java cancel 中止下载/不弹系统安装器/清理缓存
- 完成后弹居中失效弹窗（授权码已被作废/过期/不在台账），含二维码与微信一键复制，引导用户联系作者

### 设计原则：联网动作 = 权威核查点
- 检查更新（联网动作）发现授权异常（作废/过期/不在台账）→ 同步本地状态：清空 licenseCode + 把 store.license 切到 invalid，让记账功能自动被拦
- 正常路径静默结束，不打扰用户

### NativeAppInstaller 新增能力
- cancel()：让用户放弃时中止当前下载循环、跳过系统安装器调用
- clearCache()：清空已下载的 APK 缓存文件，不留残留

***

## V1.0.22 (2026-09-07)

### 修复：安卓安装后启动白屏（紧急）
- **V1.0.21 安装后白屏的修复**：构建资源路径按部署目标区分——APK 和热更新包改用相对路径，在线网页版保持绝对路径
- V1.0.21 用户请重新覆盖安装本 APK（微信传给手机直接装），记账数据都在本地不受影响

### 修复：网页版检查更新失败
- 网页版检查更新在 GitHub API 访问超时（国内网络常见）时，自动切换 jsDelivr 国内可达通道获取版本信息

***

## V1.0.21 (2026-09-07)

### 修复：安卓返回手势
- 修复安卓手机在二级页面（记一笔、分类管理等）从屏幕边缘侧滑返回时直接退出 App 的问题
- 返回手势统一规则：二级页面返回上一页（与左上角返回按钮一致），全部流水/报表/设置页直接回到首页，首页连续两次返回才退出
- 底部导航「记一笔」文字改为普通颜色，并与两侧文字水平对齐
- 本次包含原生组件更新，需下载安装新 APK（热更新无法获得此修复）

***

## V1.0.20 (2026-09-07)

### 改进：解绑与联网确认
- 解绑授权码改为联网解绑：换设备可自助完成——在原设备点「解绑」（需联网），然后在新设备输入原授权码激活，无需再联系作者
- 激活、解绑、检查更新三个联网操作均增加二次确认弹窗，点「确认」后才会联网，取消则不发起任何请求
- 解绑失败（如断网）时保留授权状态，本机与云端保持一致
- 更新页精简：删除「检查更新」按钮下方的安全说明文案

***

## V1.0.19 (2026-09-05)

### 重要变更：授权码升级为一次性激活
- 授权码改为「一码一用户、一码一机」：一个授权码只能绑定一台设备，首次激活时联网验证并绑定，防止转发分享
- 激活需要联网一次，激活成功后记账仍然完全离线可用
- 「检查更新」时会顺带联网核验授权状态（仅上传授权码与设备标识，不上传任何账单数据）
- 换设备、换浏览器或授权码失效时，请联系作者重发新码
- 已激活的老用户不受影响，记账功能照常使用

***

## V1.0.20 (2026-09-03)

### 修复

- 修复在线 PWA 深层路由刷新白屏（如 `/settings/license` 刷新后空白）：`vite.config.ts` 的 `base` 改为 `/app-release/` 绝对路径，确保子路径部署下资源引用正确

- 修复在线 PWA「检查更新」误走离线单文件更新通道：`decideUpdate` 函数根据 `location.protocol` 区分，仅 `file:` 协议走单文件更新，`https:` 协议走 ServiceWorker 更新

### 移除

- 下线离线单文件 HTML zip 产物（V1.0.19 及更早的 `随手记网页版-*.zip`）：iPhone 无法打开本地 HTML 文件，安卓有 APK，单文件通道失去使用场景

***

## V1.0.19 (2026-08-28)

> 2026-09-03 勘误：本版本在线 PWA 存在两处缺陷，随 V1.0.20 一并修复——
> ① 深层路由（如 /settings/license）刷新后白屏：资源相对路径在子路径下解析错误
> ② 网页版「检查更新」误走单文件更新通道，下载后版本不生效
> 在 V1.0.20 发布前，受影响用户重新打开应用或完整关闭后重开即可恢复正常使用。

### 修复

- 修复发布脚本 bug：Android 热更新包（webBundle）和 APK 内嵌 UI 实际打包了旧版本 dist/ 目录，导致用户更新后仍显示旧版本。现在在 `release:artifacts` 内显式先跑 `npm run build` 重建 dist/ 后再打包

### 改进

- 网页版「更新版本」页更新流程改为一键自动：点「检查更新」→ 联网确认 → 自动检查 → 发现新版本直接下载并安装 → 完成后自动重载，不再需要中间的「立即更新」按钮

- 移除「恢复内置版本」逃生舱按钮（之前从未用过，且容易让界面变复杂）

***

## V1.0.18 (2026-08-28)

### 修复

- 内部构建版本，未对外分发

***

## V1.0.17 (2026-08-28)

### 修复

- 内部构建版本，未对外分发

***

## V1.0.16 (2026-08-28)

### 修复

- 授权页激活后「当前授权码」卡片不显示授权码内容的问题（激活成功后内存状态未同步，重新进入页面才能看到）

***

## V1.0.15 (2026-08-28)

### 改进

- 授权页激活后显示当前授权码并支持复制，换设备/浏览器时自行复制重输即可恢复

***

## V1.0.14 (2026-08-28)

### 修复

- 网页版更新时可能一直停留在「正在更新中」的问题：下载增加超时与多镜像自动切换，失败时给出明确提示

***

## V1.0.13 (2026-08-28)

### 修复

- 网页版二维码图片不显示的问题（图片内嵌进单文件 HTML，此前自单文件打包起就缺失）

### 改进

- 取消授权码设备指纹绑定：输码即激活，更换浏览器/设备后重新输入同一授权码即可，无需再找作者补码

- 授权页简化：删除设备指纹卡片，激活后显示有效期

- 发码工具简化：一条命令直接生成授权码（可选到期日）

- 已发出的旧绑定授权码继续可用（兼容处理）

***

## V1.0.12 (2026-08-28)

### 修复

- 安卓点「一键备份 / 仅导出 CSV」报权限错误（EACCES）的问题

- 修复分享环节文件路径传参错误

### 新功能

- 新增「备份到手机本地」：备份文件直存手机「下载」文件夹（文件管理器可见，无需任何权限）

- 备份页按钮拆分：备份到手机本地（仅安卓）/ 备份并分享

- 首页备份提醒横幅的一键备份：安卓直接存「下载」文件夹，网页版走系统分享

### 改进

- 网页版点「备份到手机本地」给出引导提示（浏览器限制无法直存手机文件夹）

- 网页版「备份并分享」不再重复下载两次

- 备份提醒、数据备份与恢复合并为同一设置页

- 版本号起号规则变更：第 2 位满 10 进 1、第 3 位满 20 进 1（本版起号 1.0.12）

***

## V1.1.1 (2026-08-27)

### 新功能

- 首页新增备份提醒横幅：未备份流水达到阈值时顶部显示提示，点一键备份调起系统分享

- 备份提醒阈值可在设置中自定义（默认 50，范围 1\~100）

### 改进

- 备份走系统分享面板（安卓 Capacitor Share / 网页 navigator.share），无需翻文件管理器

- 网页版启动时申请持久化存储，防止手机空间不足时系统自动清理账本数据

***

## V1.1.0 (2026-08-27)

### 新功能

- 网页版支持自动更新：一次安装永久免换包，点「检查更新」即可升级（壳+本体方案，新版自动下载存入浏览器本地数据库）

- 网页版更新增加「恢复内置版本」入口，更新失败可一键回退

### 改进

- 网页版安装包内文件名改为 `随手记.html`（提醒用户长期保留同一文件）

- 发布流程全自动化：`npm run release:upload` 一条命令完成 cdn/ 推送 + Release 创建 + 资产上传

- 网页版更新通道：GitHub API 查版本 + jsDelivr CDN 下载（GitHub 直链无 CORS 头，浏览器无法直连）

***

## V1.0.9 (2026-08-27)

### 新功能

- 离线网页版改为单 HTML 文件打包，修复 file:// 协议下白屏问题

### 改进

- 发布产物命名规范统一：`随手记-app-<版本号>.apk` / `随手记-web-<版本号>.zip`

- 发布目录只保留中文名文件，GitHub 上传时临时生成英文名副本

- 完善发布流程文档与工具脚本

***

## V1.0.8 (2026-08-26)

### 修复

- 更换更新签名密钥对，恢复误删的密钥与 SDK 配置

- 修复密钥丢失导致的更新校验问题

***

## V1.0.7 (2026-08-25)

### 修复

- 彻底修复热更新瞬间数据打开失败（连接重建）

***

## V1.0.6 (2026-08-24)

### 修复

- 热更新后偶发数据打开失败自动恢复

- 备份文件固定名称不再堆积

***

## V1.0.5 (2026-08-23)

### 新功能

- 检查更新前增加联网确认弹窗

***

## V1.0.4 (2026-08-22)

### 修复

- 修复流水页分组标题显示

- 统一全局提示样式

- 完善分类删除与备份功能

